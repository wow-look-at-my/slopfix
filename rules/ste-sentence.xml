<?xml version="1.0" encoding="UTF-8"?>
<!--
  What the document rules write. The repairs themselves are sentence surgery
  rather than a phrase swap, so this file carries cases alone: a sentence as
  somebody writes it, and the sentence the repair must leave behind.

  A case whose expect repeats its test names prose the repair declines. Those
  are the cases that hold the repair inside its rule, and dropping one lets a
  wider match through unnoticed.
-->
<rules for="ste">

  <case test="It doesn't run." expect="It does not run."/>
  <case test="Don't run it. It's late." expect="Do not run it. It is late."/>
  <case test="A caller should wait. It might fail." expect="A caller must wait. It can fail."/>

  <case test="The gate is shut; the write fails." expect="The gate is shut. The write fails."/>
  <case test="The gate is shut;" expect="The gate is shut."/>

  <case test="The gate is shut, so the write fails." expect="The gate is shut. So the write fails."/>
  <case test="A caller waits, and it might fail." expect="A caller waits. And it can fail."/>
  <case test="Under the gate, the write fails." expect="Under the gate, the write fails."/>
  <case test="The rules cover a semicolon, a modal, and a splice."
        expect="The rules cover a semicolon, a modal, and a splice."/>

  <case test="Write &amp;amp; for it. It's data." expect="Write &amp;amp; for it. It is data."/>
  <case test="Write `a; b` for it. It doesn't run." expect="Write `a; b` for it. It does not run."/>
  <case test="The [it's](https://x/it's) page is not prose."
        expect="The [it is](https://x/it's) page is not prose."/>

  <case test="The gate reads every file in the session and refuses the write when any one of them carries a finding that a rewrite cannot repair on its own."
        expect="The gate reads every file in the session and refuses the write when any one of them carries a finding that a rewrite cannot repair on its own."/>
  <case test="The gate reads every file in the session and the write fails when any one of them carries a finding that a rewrite cannot repair on its own."
        expect="The gate reads every file in the session. The write fails when any one of them carries a finding that a rewrite cannot repair on its own."/>

  <case test="A short sentence that breaks no rule." expect="A short sentence that breaks no rule."/>

</rules>
