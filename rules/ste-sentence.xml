<?xml version="1.1" encoding="UTF-8"?>
<!--
  What the document rules write for a sentence. The repairs are sentence
  surgery rather than a phrase swap, so this file carries no entries: every
  test here belongs to the consumer, and drives ste.Fix as a whole.

  A test whose out repeats its in names prose the repair declines. Those hold
  each repair inside its own rule, and dropping one lets a wider match through
  unnoticed.
-->
<rules xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="rules.xsd" for="ste">

  <test in="It doesn't run." out="It does not run."/>
  <test in="Don't run it. It's late." out="Do not run it. It is late."/>
  <test in="A caller should wait. It might fail." out="A caller must wait. It can fail."/>

  <test in="The gate is shut; the write fails." out="The gate is shut. The write fails."/>
  <test in="The gate is shut;" out="The gate is shut."/>

  <test in="The gate is shut, so the write fails." out="The gate is shut. As a result, the write fails."/>
  <test in="A caller waits, and it might fail." out="A caller waits. It can fail."/>
  <test in="The gate is shut, but the write still fails." out="The gate is shut. However, the write still fails."/>
  <test in="The gate is shut, then the write fails." out="The gate is shut. Then the write fails."/>
  <test in="Under the gate, the write fails." out="Under the gate, the write fails."/>
  <test in="The rules cover a semicolon, a modal, and a splice."
        out="The rules cover a semicolon, a modal, and a splice."/>

  <test in="Write &amp;amp; for it. It's data." out="Write &amp;amp; for it. It is data."/>
  <test in="Write `a; b` for it. It doesn't run." out="Write `a; b` for it. It does not run."/>
  <test in="The [it's](https://x/it's) page is not prose."
        out="The [it is](https://x/it's) page is not prose."/>

  <test in="The gate reads every file in the session and refuses the write when any one of them carries a finding that a rewrite cannot repair on its own."
        out="The gate reads every file in the session. The gate refuses the write when any one of them carries a finding that a rewrite cannot repair on its own."/>
  <test in="The loader opens the file and reads every row it holds into memory, which is the whole reason a caller waits on it before the header check runs."
        out="The loader opens the file and reads every row it holds into memory. This is the whole reason a caller waits on it before the header check runs."/>
  <test in="The gate reads every file in the session and the write fails when any one of them carries a finding that a rewrite cannot repair on its own."
        out="The gate reads every file in the session. The write fails when any one of them carries a finding that a rewrite cannot repair on its own."/>

  <test in="The build fails on every runner in the fleet whenever the cache holds a stale tree because the key names the lockfile and nothing else in the checkout."
        out="The build fails on every runner in the fleet whenever the cache holds a stale tree. This is because the key names the lockfile and nothing else in the checkout."/>
  <test in="Write a period in place of the comma that joins the clauses and start the next sentence with a capital so the reader sees where each thought ends."
        out="Write a period in place of the comma that joins the clauses. Start the next sentence with a capital so the reader sees where each thought ends."/>
  <test in="The long list of every cached artifact in the store reads the index from disk and writes a fresh copy of it back whenever an entry changes its hash."
        out="The long list of every cached artifact in the store reads the index from disk. It writes a fresh copy of it back whenever an entry changes its hash."/>

  <test in="`sandbox: false` and `enabled:` were both removed from the parser in the release and are now parse errors that name the key the file still carries today."
        out="`sandbox: false` and `enabled:` were both removed from the parser in the release. They are now parse errors that name the key the file still carries today."/>
  <test in="A user who cannot dismiss a message reads it as contempt and reads nothing else that the whole surface shows after a few minutes of use."
        out="A user who cannot dismiss a message reads it as contempt. The user reads nothing else that the whole surface shows after a few minutes of use."/>

  <!-- The end of a list, a quotation and a status code are not what they look like. -->
  <test in="The report names the cache, the lockfile, the tree that the build reads from the disk, and the runner that holds the job for every run in the fleet."
        out="The report names the cache, the lockfile, the tree that the build reads from the disk, and the runner that holds the job for every run in the fleet."/>
  <test in="The owner wrote &quot;the three rules run&quot; in the log." out="The owner wrote &quot;the three rules run&quot; in the log."/>
  <test in="This 403 happens on every push." out="This 403 happens on every push."/>
  <test in="If the change makes a screen emptier, you have removed a feature." out="If the change makes a screen emptier, you have removed a feature."/>
  <test in="On a hosted runner, where each run gets its own machine, a concurrency group is optional."
        out="On a hosted runner, where each run gets its own machine, a concurrency group is optional."/>
  <test in="The deletion can return a 403 in a session. In that case, a maintainer deletes the branch."
        out="The deletion can return a 403 in a session. In that case, a maintainer deletes the branch."/>
  <test in="`''`, `0`, and an empty list are each a lie." out="`''`, `0`, and an empty list are each a lie."/>

  <!-- No clause boundary: the sentence stays whole, and Check reports it. -->
  <test in="A reader arriving at this paragraph without any conjunction anywhere inside its single enormous run-on clause still deserves a repair from the tool rather than a deletion."
        out="A reader arriving at this paragraph without any conjunction anywhere inside its single enormous run-on clause still deserves a repair from the tool rather than a deletion."/>

  <!-- A numeral between a determiner and its noun restates a count. -->
  <test in="The three rules run on every file." out="The rules run on every file."/>
  <test in="Its four fields hold the header." out="Its fields hold the header."/>
  <test in="The repository's two branches build." out="The repository's branches build."/>
  <test in="These 12 checks gate the merge." out="These checks gate the merge."/>
  <test in="The one file that matters is the lockfile." out="The file that matters is the lockfile."/>
  <test in="The first two steps run in parallel." out="The first two steps run in parallel."/>
  <test in="Any two nodes share an edge." out="Any two nodes share an edge."/>
  <test in="The 30 seconds pass before the retry." out="The 30 seconds pass before the retry."/>
  <test in="The 2026 release ships the parser." out="The 2026 release ships the parser."/>
  <test in="The two are equal." out="The two are equal."/>
  <test in="The `x` flag takes the three values below." out="The `x` flag takes the values below."/>

  <test in="A short sentence that breaks no rule." out="A short sentence that breaks no rule."/>

  <!-- go-toolchain docs. The commas inside the parentheses separate list items, and the code span opens its own sentence. -->
  <test in="Only the target's own artifacts are touched: the bare name (`&lt;name&gt;.exe` and the fat APE), every `&lt;name&gt;_…` shape the toolchain writes (`&lt;name&gt;_&lt;goos&gt;_&lt;goarch&gt;`, the wasm names, the `&lt;name&gt;_host` symlink), and the APE's `&lt;name&gt;.…` sidecar ELFs. `checksums.txt`, `wasm_exec.js`, `profile.json` and anything else in `build/` are left alone."
        out="Only the target's own artifacts are touched: the bare name (`&lt;name&gt;.exe` and the fat APE), every `&lt;name&gt;_…` shape the toolchain writes (`&lt;name&gt;_&lt;goos&gt;_&lt;goarch&gt;`, the wasm names, the `&lt;name&gt;_host` symlink), and the APE's `&lt;name&gt;.…` sidecar ELFs. `checksums.txt`, `wasm_exec.js`, `profile.json` and anything else in `build/` are left alone."/>
  <!-- go-toolchain docs. A period inside bold ends its sentence, so the next one is measured alone. -->
  <test in="**This is the only name saved.** The pre-build per-job name `go-build-&lt;job&gt;[.m&lt;idx&gt;]` and the bare `go-build` alias are gone: each was a second key that a multi-producer run raced on."
        out="**This is the only name saved.** The pre-build per-job name `go-build-&lt;job&gt;[.m&lt;idx&gt;]` and the bare `go-build` alias are gone. Each was a second key that a multi-producer run raced on."/>
  <test in="**Writing the scan out by hand does not escape the check.** A loop over a candidate whose body is one `if` comparing the element to a value."
        out="**Writing the scan out by hand does not escape the check.** A loop over a candidate whose body is one `if` comparing the element to a value."/>
  <test in="**Reproducible across build hosts.** Every build passes `-trimpath` and `-ldflags=-buildid=`, so the same source compiles to the same bytes wherever it is built."
        out="**Reproducible across build hosts.** Every build passes `-trimpath` and `-ldflags=-buildid=`, so the same source compiles to the same bytes wherever it is built."/>
  <!-- go-toolchain docs. A spaced dash after a clause is a seam, and the words after it keep their order. -->
  <test in="That last check is the one that stays honest over time — a stray `&lt;name&gt;_&lt;os&gt;_&lt;arch&gt;` file will silently restore the N-downloads-of-one-binary shape without failing anything else."
        out="That last check is the one that stays honest over time. A stray `&lt;name&gt;_&lt;os&gt;_&lt;arch&gt;` file will silently restore the N-downloads-of-one-binary shape without failing anything else."/>
  <!-- go-toolchain docs. "(e.g." does not end a sentence, so the division lands on the dash rather than inside the quote. -->
  <test in="It is deliberately scoped to `go: ` lines rather than &quot;any duration under 1s anywhere in the log&quot;: go-toolchain's own named step/test timers (e.g. `vet: gofmt 0.17s`) are intentionally unconditional — a named operation's own time is always worth reporting — and must not be flagged."
        out="It is deliberately scoped to `go: ` lines rather than &quot;any duration under 1s anywhere in the log&quot;. Go-toolchain's own named step/test timers (e.g. `vet: gofmt 0.17s`) are intentionally unconditional — a named operation's own time is always worth reporting — and must not be flagged."/>
  <!-- go-toolchain docs. A code span that opens a sentence opens it for the repair too, so the splice after it divides. -->
  <test in="Must exist, and must begin with the APE magic. `apeManifestEntries` checks both before writing, so a broken manifest is caught where the artifact names are still in hand."
        out="Must exist, and must begin with the APE magic. `apeManifestEntries` checks both before writing. As a result, a broken manifest is caught where the artifact names are still in hand."/>
  <!-- go-toolchain docs. The clause after the colon resumes after its relative clause, and still names its own subject. -->
  <test in="The check runs on every module go-toolchain builds, its own and every consumer's: a map that carries no information is wasteful wherever it is written."
        out="The check runs on every module go-toolchain builds, its own and every consumer's. A map that carries no information is wasteful wherever it is written."/>
  <test in="**Automatic GOMEMLIMIT** — the compiler's runtime caps every binary's Go heap at the container's cgroup limit instead of being OOM-killed. `GOMEMLIMIT=off` opts out at run time."
        out="**Automatic GOMEMLIMIT** — the compiler's runtime caps every binary's Go heap at the container's cgroup limit instead of being OOM-killed. `GOMEMLIMIT=off` opts out at run time."/>

</rules>
